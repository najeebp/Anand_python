#Write a function valuesort to sort values of a dictionary based on the key.
a={'x':1,'y':2,'a':3}
def valuesort(a):
	#b=a.items()
	#u=b.sort()
	#print b,u
	p=[]
	#m=range(len(u))
	#for q in m:
	#	p.append(u[q][1])
	#print p
	for key,value in a.items():
		print p.append(key) 
	#print s[0]	
valuesort(a)
